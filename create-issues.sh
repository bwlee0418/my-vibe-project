#!/bin/bash
# GitHub 이슈 자동 등록 스크립트
# 실행 방법: cd ~/Documents/my-vibe-project && bash create-issues.sh

echo "🏷️ 라벨 생성 중..."
gh label create "MVP" --description "MVP 필수 기능" --color "d73a4a" 2>/dev/null
gh label create "enhancement" --description "향후 확장 기능" --color "a2eeef" 2>/dev/null
gh label create "frontend" --description "프론트엔드 작업" --color "7057ff" 2>/dev/null
gh label create "backend" --description "백엔드/API 작업" --color "0075ca" 2>/dev/null
gh label create "design" --description "디자인/UI 작업" --color "e4e669" 2>/dev/null
gh label create "data" --description "데이터 저장/관리" --color "fbca04" 2>/dev/null

echo ""
echo "📋 이슈 등록 중..."

# Issue 1
gh issue create --title "프로젝트 구조 및 기본 레이아웃 설정" \
  --label "MVP,frontend" \
  --body "## 배경
프로젝트의 기본 구조를 잡고 공통 레이아웃을 구현합니다.

## 작업 내용
- 프로젝트 디렉토리 구조 설정
- index.html 기본 레이아웃 작성
- Tailwind CSS CDN 연동
- 공통 스타일 정의

## 인수 조건
- [ ] index.html 파일이 브라우저에서 정상 렌더링
- [ ] Tailwind CSS가 적용되어 스타일링 동작
- [ ] 반응형 기본 구조 확인

## 의존성
없음 (첫 번째 작업)"

# Issue 2
gh issue create --title "네비게이션 및 페이지 라우팅 구현" \
  --label "MVP,frontend" \
  --body "## 배경
회의록, 보고서, CX 지표 세 가지 주요 페이지 간 전환이 가능해야 합니다.

## 작업 내용
- 상단 네비게이션 바 구현 (데스크톱 + 모바일)
- 페이지 전환 로직 (SPA 방식, JS로 show/hide)
- 현재 페이지 활성 상태 표시
- 사용자 프로필 영역

## 인수 조건
- [ ] 세 개 페이지 간 전환이 매끄럽게 동작
- [ ] 모바일에서 탭 네비게이션 정상 동작
- [ ] 현재 활성 페이지가 시각적으로 구분됨

## 의존성
- #1 (프로젝트 구조 설정 완료 후)"

# Issue 3
gh issue create --title "음성 녹음 기능 구현 (Web Speech API)" \
  --label "MVP,frontend" \
  --body "## 배경
사용자가 회의를 직접 녹음하고 실시간으로 텍스트로 변환할 수 있어야 합니다.

## 작업 내용
- Web Speech API (SpeechRecognition) 연동
- 녹음 시작/중지 버튼
- 녹음 시간 표시
- 실시간 텍스트 변환 결과를 입력 영역에 표시
- 녹음 중 시각적 피드백 (펄스 애니메이션)
- 마이크 권한 요청 처리

## 인수 조건
- [ ] 녹음 시작/중지가 정상 동작
- [ ] 한국어 음성이 텍스트로 변환됨
- [ ] 변환된 텍스트가 회의 입력 영역에 실시간 표시
- [ ] 녹음 시간이 정확히 표시됨

## 의존성
- #1 (프로젝트 구조 설정 완료 후)"

# Issue 4
gh issue create --title "회의 텍스트 입력 UI 구현" \
  --label "MVP,frontend,design" \
  --body "## 배경
음성 변환 결과 또는 직접 붙여넣기한 텍스트를 입력받는 UI가 필요합니다.

## 작업 내용
- 회의 정보 입력 영역 (일시, 참석자)
- 텍스트 입력 영역 (textarea)
- 음성 변환 결과 자동 반영
- 직접 텍스트 붙여넣기 지원
- AI 정리 요청 버튼

## 인수 조건
- [ ] 회의 일시와 참석자를 입력할 수 있음
- [ ] 텍스트 영역에 직접 입력 및 붙여넣기 가능
- [ ] UI가 깔끔하고 직관적

## 의존성
- #2 (네비게이션 구현 후)"

# Issue 5
gh issue create --title "AI 회의록 자동 정리 기능 구현" \
  --label "MVP,backend" \
  --body "## 배경
입력된 회의 텍스트를 AI가 분석하여 구조화된 회의록으로 자동 정리하는 핵심 기능입니다.

## 작업 내용
- AI API 연동 (OpenAI 또는 Claude API)
- 프롬프트 설계: 회의 텍스트 → 구조화된 회의록 변환
  - 회의 기본 정보 추출
  - 안건별 논의 내용 요약
  - 결정사항 목록 추출
  - 액션 아이템 추출 (담당자, 기한, 내용)
- API 키 관리 (환경변수 또는 설정 입력)
- 로딩 상태 표시
- 에러 처리

## 인수 조건
- [ ] 회의 텍스트를 입력하면 구조화된 JSON 결과 반환
- [ ] 안건, 결정사항, 액션아이템이 정확히 분류됨
- [ ] API 호출 중 로딩 상태 표시
- [ ] 에러 발생 시 사용자에게 안내

## 의존성
- #4 (회의 텍스트 입력 UI 완료 후)"

# Issue 6
gh issue create --title "회의록 결과 표시 UI 구현" \
  --label "MVP,frontend,design" \
  --body "## 배경
AI가 정리한 회의록을 사용자가 보기 좋게 표시하고 수정할 수 있어야 합니다.

## 작업 내용
- 회의 정보 요약 카드 (일시, 참석자, 주제)
- 안건별 논의 내용 목록
- 결정사항 하이라이트 섹션
- 액션 아이템 체크리스트 (담당자, 기한)
- 수정 기능
- 저장 버튼

## 인수 조건
- [ ] AI 정리 결과가 구조화된 형태로 표시
- [ ] 각 섹션이 시각적으로 구분됨
- [ ] 액션 아이템 체크박스 동작
- [ ] 수정 및 저장 버튼 동작

## 의존성
- #5 (AI 회의록 정리 기능 완료 후)"

# Issue 7
gh issue create --title "CX 지표 입력 폼 구현" \
  --label "MVP,frontend" \
  --body "## 배경
CX 팀의 일일 지표를 입력하는 폼이 필요합니다. 이 데이터는 보고서 자동 생성에 활용됩니다.

## 작업 내용
- 날짜 선택
- 문의 유형별 건수 입력 (전화, 이메일, 채팅, 기타)
- 처리 현황 입력 (해결, 진행 중, 미해결)
- 주요 이슈/특이사항 텍스트 입력
- 저장 버튼
- 이번 주 요약 표시

## 인수 조건
- [ ] 각 입력 필드가 정상 동작
- [ ] 저장 버튼 클릭 시 데이터 저장
- [ ] 입력한 데이터 기반으로 요약 표시

## 의존성
- #2 (네비게이션 구현 후)"

# Issue 8
gh issue create --title "주간 보고서 자동 생성 기능" \
  --label "MVP,backend" \
  --body "## 배경
축적된 회의록과 CX 지표 데이터를 기반으로 주간 보고서를 자동 생성하는 핵심 기능입니다.

## 작업 내용
- 보고서 생성 설정 UI (기간, 포함 데이터 선택)
- 해당 기간 데이터 취합 로직
- AI API 연동: 데이터 → 보고서 초안 생성
  - 주요 활동 요약
  - CX 지표 현황 (수치 + 변화율)
  - 주요 이슈 및 특이사항
  - 다음 주 계획
- 보고서 템플릿 적용

## 인수 조건
- [ ] 기간 선택 후 보고서 생성 가능
- [ ] 보고서에 CX 지표와 회의 내용이 통합 반영
- [ ] 보고서 양식이 일관됨
- [ ] 생성 시간 30초 이내

## 의존성
- #5 (AI 회의록 정리 기능)
- #7 (CX 지표 입력 폼)"

# Issue 9
gh issue create --title "보고서 미리보기 및 복사/다운로드" \
  --label "MVP,frontend" \
  --body "## 배경
생성된 보고서를 사용자가 미리 확인하고, 수정 후 내보낼 수 있어야 합니다.

## 작업 내용
- 보고서 미리보기 화면 (서식 적용)
- 보고서 내용 수정 기능
- 클립보드 복사 (마크다운 형식)
- 마크다운 파일 다운로드
- 보고서 메타 정보 표시 (작성일, 기간, 작성자)

## 인수 조건
- [ ] 보고서가 깔끔한 형식으로 미리보기 표시
- [ ] 복사 버튼 클릭 시 클립보드에 복사
- [ ] 다운로드 버튼 클릭 시 .md 파일 저장
- [ ] 수정 사항이 즉시 반영

## 의존성
- #8 (주간 보고서 생성 기능 완료 후)"

# Issue 10
gh issue create --title "IndexedDB 데이터 저장 레이어 구현" \
  --label "enhancement,data" \
  --body "## 배경
서버 없이 브라우저 내부에 데이터를 영속적으로 저장하기 위해 IndexedDB를 사용합니다.

## 작업 내용
- IndexedDB 초기화 및 스키마 정의
- 회의록 CRUD 함수
- CX 지표 CRUD 함수
- 보고서 CRUD 함수
- 데이터 마이그레이션 유틸리티

## 인수 조건
- [ ] 데이터 저장/조회/수정/삭제가 정상 동작
- [ ] 브라우저 새로고침 후에도 데이터 유지
- [ ] 에러 핸들링 구현

## 의존성
- #1 (프로젝트 구조 설정 완료 후)"

# Issue 11
gh issue create --title "회의록 이력 관리 및 검색" \
  --label "enhancement,frontend" \
  --body "## 배경
저장된 회의록을 목록으로 조회하고 검색할 수 있어야 합니다.

## 작업 내용
- 저장된 회의록 목록 화면
- 날짜/키워드 기반 검색
- 회의록 상세 보기
- 회의록 삭제 기능

## 인수 조건
- [ ] 저장된 회의록이 목록으로 표시
- [ ] 검색 시 관련 회의록 필터링
- [ ] 클릭 시 상세 내용 확인 가능

## 의존성
- #6 (회의록 결과 표시 UI)
- #10 (IndexedDB 저장 레이어)"

echo ""
echo "✅ 모든 이슈가 등록되었습니다!"
echo ""
echo "📋 등록된 이슈 확인:"
gh issue list
